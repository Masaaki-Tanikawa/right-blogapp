= link_to article_path(article) do
  .card
    - if article.eyecatch.attached? # アイキャッチがアップロードされた場合のみ、画像を表示
      .card_image
        = image_tag article.eyecatch
    .card_content
      .card_title= article.title
      .card_heart
        = image_tag 'heart.svg'
        %span= article.like_count # いいね数を動的に表示
      .card_detail
        = image_tag article.user.avatar_image # 記事を作成したユーザーのアバター画像を設定
        %div
          %p= article.user.display_name
          %p= article.display_created_at
